syntax = "proto3";

package ai_service;

service AIService {
    rpc QA (Question) returns (stream Answer) {}
}

message Question {
    string question = 1;
}

message Answer {
    string answer = 1;
}